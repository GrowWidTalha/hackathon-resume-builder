{
  "name": "resume-builder",
  "version": "1.0.0",
"type": "module",
  "scripts": {
    "build": "tsc && mkdir -p dist/css dist/js && cp src/css/style.css dist/css && cp index.html dist && cp -r public dist/public && cp src/js/*.js dist/js"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "mkdirp": "^1.0.4",
    "copyfiles": "^2.4.1"
  }
}