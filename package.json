{
  "name": "resume-builder",
  "version": "1.0.0",
"type": "module",
  "scripts": {
    "build": "tsc && mkdirp dist/css && copyfiles -u 1 src/css/style.css dist/css && copyfiles -u 0 index.html dist && copyfiles -u 1 public/**/* dist/public"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "mkdirp": "^1.0.4",
    "copyfiles": "^2.4.1"
  }
}