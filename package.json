{
  "name": "resume-builder",
  "version": "1.0.0",
"type": "module",
  "scripts": {
    "build": "rimraf dist && npx mkdirp dist/js dist/css && tsc && npx copyfiles src/css/style.css dist/css && npx copyfiles index.html dist && npx copyfiles -u 1 public/**/* dist/public"
  },
  "devDependencies": {
"rimraf": "^5.0.0",
    "typescript": "^4.0.0",
    "mkdirp": "^1.0.4",
    "copyfiles": "^2.4.1"
  }
}